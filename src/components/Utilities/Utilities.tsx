import { PostMessageListener, RedirectWebviewLogin } from './components';

export const Utilities = () => {
  return (
    <>
      <PostMessageListener />
      <RedirectWebviewLogin />
    </>
  );
};
